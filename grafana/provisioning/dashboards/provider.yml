# Dashboard provisioning: auto-load every JSON in /var/lib/grafana/dashboards.
apiVersion: 1

providers:
  - name: industrial
    folder: Industrial
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
